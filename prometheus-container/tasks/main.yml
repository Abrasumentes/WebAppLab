---
# tasks file for prometheus-container
    - name: Start Prometheus container
      docker_container:
        name: prometheus
        image: prom/prometheus
        ports:
          - "9090:9090"
        volumes: 
          - "/etc/prometheus/:/home/abra/DockerUbuntu/WebApp"
        restart_policy: always
